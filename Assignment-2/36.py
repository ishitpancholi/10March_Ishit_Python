#python program calculate area of parallelogram

base_1=6
base_2=7

height=float(input("Height of parallelogram:"))
base_1=float(input("base one value:"))
base_2=float(input("base two value"))

area=(base_1*base_2*height)
print("Area is:",area)
